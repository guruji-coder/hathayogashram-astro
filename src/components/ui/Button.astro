---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className = '' } = Astro.props;
---

<a
  href={href}
  class={`inline-block cursor-pointer ${className}`}
>
  <slot />
</a>
