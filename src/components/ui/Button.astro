---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className = '' } = Astro.props;
---

<a
  href={href}
  class={`inline-block rounded-md bg-primary px-6 py-3 text-white hover:bg-primary/90 transition-colors ${className}`}
>
  <slot />
</a>
